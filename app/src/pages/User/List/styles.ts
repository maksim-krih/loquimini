import {createUseStyles} from 'react-jss'

export const useStyles = createUseStyles({
  container: {
    
  },
  actionButtons: {
    marginBottom: 10
  },
  gridButton: {
    padding: 0,
    width: 34,
    borderRadius: 5
  }
})