import {createUseStyles} from 'react-jss'

export const useStyles = createUseStyles({
  container: {
    backgroundColor: "#001529",
    position: "fixed",
    width: "100%",
    height: "100%"
  },
  login: {
    padding: "0 24px",
    width: 300,
    margin: "auto",
    marginTop: 300
  }
})